using Newtonsoft.Json;
using Newtonsoft.Json.Linq;
using RedflyCoreFramework;
using redflyDatabaseAdapters;
using redflyGeneratedDataAccessApi.Base;
using redflyGeneratedDataAccessApi.Common;
using redflyGeneratedDataAccessApi.Protos.DatabaseApi;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace redflyGeneratedDataAccessApi.Postgres.AdventureWorks;

// Strongly-typed classes for [humanresources].[jobcandidate] generated by the
// redfly PostgresGrpcPolyLangCompiler
// This is only meant to be indicative of the features available in the core product.

public class HumanresourcesJobcandidate : BasePostgresTableSchema
{
    public int Jobcandidateid { get; set; }
    public int? Businessentityid { get; set; }
    public string? Resume { get; set; }
    public DateTime Modifieddate { get; set; }
}

public class HumanresourcesJobcandidateRowsData : BaseTableRowsData
{
    public List<HumanresourcesJobcandidate> Rows { get; set; } = new();
}

public class HumanresourcesJobcandidateInsertedData : BaseTableInsertedData
{
    public HumanresourcesJobcandidate? InsertedRow { get; set; }
}

public class HumanresourcesJobcandidateRowData : BaseTableRowData
{
    public HumanresourcesJobcandidate? Row { get; set; }
}

public class HumanresourcesJobcandidateDataSource : BasePostgresTableDataSource<HumanresourcesJobcandidate>
{
    public HumanresourcesJobcandidateDataSource() : base()
    {
        _encSchema = RedflyEncryption.EncryptToString("humanresources");
        _encTable = RedflyEncryption.EncryptToString("jobcandidate");
    }

    public async Task<DeletedData> DeleteAsync(int jobcandidateid, bool modifyCache = true)
    {
        var req = base.CreateDeleteRequest(modifyCache);
        req.PrimaryKeyValues.Add("jobcandidateid", jobcandidateid.ToString());
        return await base.DeleteCoreAsync(req);
    }

    public async Task<HumanresourcesJobcandidateRowsData> GetRowsAsync(int pageNo = 1, int pageSize = 50, string orderByColumnName = "", string orderBySort = "", bool useCache = true)
    {
        var req = base.CreateGetRowsRequest(pageNo, pageSize, orderByColumnName, orderBySort);
        var resp = await _client.GetRowsAsync(req, AppGrpcSession.Headers!);
        var rows = new List<HumanresourcesJobcandidate>();
        foreach (var row in resp.Rows)
        {
            rows.Add(MapRowToTableEntity(row));
        }
        return new HumanresourcesJobcandidateRowsData
        {
            Success = resp.Success,
            Rows = rows,
            FromCache = resp.FromCache,
            Message = resp.Message
        };
    }

    public async Task<HumanresourcesJobcandidateInsertedData> InsertAsync(HumanresourcesJobcandidate entity, bool modifyCache = true)
    {
        var req = base.CreateInsertRequest(entity, modifyCache);
        var resp = await _client.InsertAsync(req, AppGrpcSession.Headers!);
        return new HumanresourcesJobcandidateInsertedData
        {
            Success = resp.Success,
            InsertedRow = resp.InsertedRow != null ? MapRowToTableEntity(resp.InsertedRow) : null,
            CacheUpdated = resp.CacheUpdated,
            Message = resp.Message
        };
    }

    public async Task<HumanresourcesJobcandidateRowData> GetAsync(int jobcandidateid, bool useCache = true)
    {
        var req = base.CreateGetRequest();
        req.PrimaryKeyValues.Add("jobcandidateid", jobcandidateid.ToString());
        var resp = await _client.GetAsync(req, AppGrpcSession.Headers!);
        return new HumanresourcesJobcandidateRowData
        {
            Success = resp.Success,
            Row = resp.Row != null ? MapRowToTableEntity(resp.Row) : null,
            FromCache = resp.FromCache,
            Message = resp.Message
        };
    }

    public async Task<UpdatedData> UpdateAsync(HumanresourcesJobcandidate entity, bool modifyCache = true)
    {
        var req = CreateUpdateRequest(entity, modifyCache);
        return await UpdateCoreAsync(req);
    }

    protected override HumanresourcesJobcandidate MapRowToTableEntity(Row row)
    {
        var dict = new Dictionary<string, string?>();
        foreach (var entry in row.Entries)
        {
            dict[entry.Column] = entry.Value?.StringValue;
        }
        return new HumanresourcesJobcandidate
        {
            Jobcandidateid = dict.TryGetValue("jobcandidateid", out var v1) && !string.IsNullOrEmpty(v1) ? (int)Convert.ChangeType(v1, typeof(int)) : default(int),
            Businessentityid = dict.TryGetValue("businessentityid", out var v2) && !string.IsNullOrEmpty(v2) ? (int)Convert.ChangeType(v2, typeof(int)) : null,
            Resume = dict.TryGetValue("resume", out var v3) && !string.IsNullOrEmpty(v3) ? (string)Convert.ChangeType(v3, typeof(string)) : null,
            Modifieddate = dict.TryGetValue("modifieddate", out var v4) && !string.IsNullOrEmpty(v4) ? (DateTime)Convert.ChangeType(v4, typeof(DateTime)) : default(DateTime),
        };
    }

    protected override Row MapTableEntityToRow(HumanresourcesJobcandidate entity, DbOperationType dbOperationType)
    {
        var row = new Row();

        row.Entries.Add(new RowEntry { Column = "jobcandidateid", Value = new Value { StringValue = entity.Jobcandidateid.ToString() } });

        // For Postgres, add all non-primary key columns
        if (entity.Businessentityid != null)
        {
            row.Entries.Add(new RowEntry { Column = "businessentityid", Value = new Value { StringValue = entity.Businessentityid.ToString() } });
        }
        else
        {
            row.Entries.Add(new RowEntry { Column = "businessentityid", Value = new Value { StringValue = null } });
        }
        row.Entries.Add(new RowEntry { Column = "resume", Value = new Value { StringValue = entity.Resume } });
        if (entity.Modifieddate != DateTime.MinValue)
        {
            row.Entries.Add(new RowEntry { Column = "modifieddate", Value = new Value { StringValue = entity.Modifieddate.ToString("yyyy-MM-dd HH:mm:ss.fff") } });
        }
        return row;
    }
}
