using Newtonsoft.Json;
using Newtonsoft.Json.Linq;
using RedflyCoreFramework;
using redflyDatabaseAdapters;
using redflyGeneratedDataAccessApi.Base;
using redflyGeneratedDataAccessApi.Common;
using redflyGeneratedDataAccessApi.Protos.DatabaseApi;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace redflyGeneratedDataAccessApi.Postgres.AdventureWorks;

// Strongly-typed classes for [sales].[specialofferproduct] generated by the
// redfly PostgresGrpcPolyLangCompiler on 06/06/25 04:28:20 PM
// This is only meant to be indicative of the features available in the core product.

public class SalesSpecialofferproduct : BasePostgresTableSchema
{
    public int Specialofferid { get; set; }
    public int Productid { get; set; }
    public Guid Rowguid { get; set; }
    public DateTime Modifieddate { get; set; }
}

public class SalesSpecialofferproductRowsData : BaseTableRowsData
{
    public List<SalesSpecialofferproduct> Rows { get; set; } = new();
}

public class SalesSpecialofferproductInsertedData : BaseTableInsertedData
{
    public SalesSpecialofferproduct? InsertedRow { get; set; }
}

public class SalesSpecialofferproductRowData : BaseTableRowData
{
    public SalesSpecialofferproduct? Row { get; set; }
}

public class SalesSpecialofferproductDataSource : BasePostgresTableDataSource<SalesSpecialofferproduct>
{
    public SalesSpecialofferproductDataSource() : base()
    {
        _encSchema = RedflyEncryption.EncryptToString("sales");
        _encTable = RedflyEncryption.EncryptToString("specialofferproduct");
    }

    public async Task<DeletedData> DeleteAsync(int specialofferid, int productid, bool modifyCache = true)
    {
        var req = base.CreateDeleteRequest(modifyCache);
        req.PrimaryKeyValues.Add("specialofferid", specialofferid.ToString());
        req.PrimaryKeyValues.Add("productid", productid.ToString());
        return await base.DeleteCoreAsync(req);
    }

    public async Task<SalesSpecialofferproductRowsData> GetRowsAsync(int pageNo = 1, int pageSize = 50, string orderByColumnName = "", string orderBySort = "", bool useCache = true)
    {
        var req = base.CreateGetRowsRequest(pageNo, pageSize, orderByColumnName, orderBySort);
        var resp = await _client.GetRowsAsync(req, AppGrpcSession.Headers!);
        var rows = new List<SalesSpecialofferproduct>();
        foreach (var row in resp.Rows)
        {
            rows.Add(MapRowToTableEntity(row));
        }
        return new SalesSpecialofferproductRowsData
        {
            Success = resp.Success,
            Rows = rows,
            FromCache = resp.FromCache,
            Message = resp.Message
        };
    }

    public async Task<SalesSpecialofferproductInsertedData> InsertAsync(SalesSpecialofferproduct entity, bool modifyCache = true)
    {
        var req = base.CreateInsertRequest(entity, modifyCache);
        var resp = await _client.InsertAsync(req, AppGrpcSession.Headers!);
        return new SalesSpecialofferproductInsertedData
        {
            Success = resp.Success,
            InsertedRow = resp.InsertedRow != null ? MapRowToTableEntity(resp.InsertedRow) : null,
            CacheUpdated = resp.CacheUpdated,
            Message = resp.Message
        };
    }

    public async Task<SalesSpecialofferproductRowData> GetAsync(int specialofferid, int productid, bool useCache = true)
    {
        var req = base.CreateGetRequest();
        req.PrimaryKeyValues.Add("specialofferid", specialofferid.ToString());
        req.PrimaryKeyValues.Add("productid", productid.ToString());
        var resp = await _client.GetAsync(req, AppGrpcSession.Headers!);
        return new SalesSpecialofferproductRowData
        {
            Success = resp.Success,
            Row = resp.Row != null ? MapRowToTableEntity(resp.Row) : null,
            FromCache = resp.FromCache,
            Message = resp.Message
        };
    }

    public async Task<UpdatedData> UpdateAsync(SalesSpecialofferproduct entity, bool modifyCache = true)
    {
        var req = CreateUpdateRequest(entity, modifyCache);
        return await UpdateCoreAsync(req);
    }

    protected override SalesSpecialofferproduct MapRowToTableEntity(Row row)
    {
        var dict = new Dictionary<string, string?>();
        foreach (var entry in row.Entries)
        {
            dict[entry.Column] = entry.Value?.StringValue;
        }
        return new SalesSpecialofferproduct
        {
            Specialofferid = dict.TryGetValue("specialofferid", out var v1) && !string.IsNullOrEmpty(v1) ? (int)Convert.ChangeType(v1, typeof(int)) : default(int),
            Productid = dict.TryGetValue("productid", out var v2) && !string.IsNullOrEmpty(v2) ? (int)Convert.ChangeType(v2, typeof(int)) : default(int),
            Rowguid = dict.TryGetValue("rowguid", out var v3) && !string.IsNullOrEmpty(v3) ? (Guid)Convert.ChangeType(v3, typeof(Guid)) : default(Guid),
            Modifieddate = dict.TryGetValue("modifieddate", out var v4) && !string.IsNullOrEmpty(v4) ? (DateTime)Convert.ChangeType(v4, typeof(DateTime)) : default(DateTime),
        };
    }

    protected override Row MapTableEntityToRow(SalesSpecialofferproduct entity, DbOperationType dbOperationType)
    {
        var row = new Row();

        row.Entries.Add(new RowEntry { Column = "specialofferid", Value = new Value { StringValue = entity.Specialofferid.ToString() } });
        row.Entries.Add(new RowEntry { Column = "productid", Value = new Value { StringValue = entity.Productid.ToString() } });

        // For Postgres, add all non-primary key columns
        row.Entries.Add(new RowEntry { Column = "rowguid", Value = new Value { StringValue = entity.Rowguid.ToString() } });
        if (entity.Modifieddate != DateTime.MinValue)
        {
            row.Entries.Add(new RowEntry { Column = "modifieddate", Value = new Value { StringValue = entity.Modifieddate.ToString("yyyy-MM-dd HH:mm:ss.fff") } });
        }
        return row;
    }
}
