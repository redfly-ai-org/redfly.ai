using Newtonsoft.Json;
using Newtonsoft.Json.Linq;
using RedflyCoreFramework;
using redflyDatabaseAdapters;
using redflyGeneratedDataAccessApi.Base;
using redflyGeneratedDataAccessApi.Common;
using redflyGeneratedDataAccessApi.Protos.DatabaseApi;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace redflyGeneratedDataAccessApi.Postgres.AdventureWorks;

// Strongly-typed classes for [person].[personphone] generated by the
// redfly PostgresGrpcPolyLangCompiler on 06/06/25 04:28:17 PM
// This is only meant to be indicative of the features available in the core product.

public class PersonPersonphone : BasePostgresTableSchema
{
    public int Businessentityid { get; set; }
    public string Phonenumber { get; set; } = string.Empty;
    public int Phonenumbertypeid { get; set; }
    public DateTime Modifieddate { get; set; }
}

public class PersonPersonphoneRowsData : BaseTableRowsData
{
    public List<PersonPersonphone> Rows { get; set; } = new();
}

public class PersonPersonphoneInsertedData : BaseTableInsertedData
{
    public PersonPersonphone? InsertedRow { get; set; }
}

public class PersonPersonphoneRowData : BaseTableRowData
{
    public PersonPersonphone? Row { get; set; }
}

public class PersonPersonphoneDataSource : BasePostgresTableDataSource<PersonPersonphone>
{
    public PersonPersonphoneDataSource() : base()
    {
        _encSchema = RedflyEncryption.EncryptToString("person");
        _encTable = RedflyEncryption.EncryptToString("personphone");
    }

    public async Task<DeletedData> DeleteAsync(int businessentityid, string phonenumber, int phonenumbertypeid, bool modifyCache = true)
    {
        var req = base.CreateDeleteRequest(modifyCache);
        req.PrimaryKeyValues.Add("businessentityid", businessentityid.ToString());
        req.PrimaryKeyValues.Add("phonenumber", phonenumber.ToString());
        req.PrimaryKeyValues.Add("phonenumbertypeid", phonenumbertypeid.ToString());
        return await base.DeleteCoreAsync(req);
    }

    public async Task<PersonPersonphoneRowsData> GetRowsAsync(int pageNo = 1, int pageSize = 50, string orderByColumnName = "", string orderBySort = "", bool useCache = true)
    {
        var req = base.CreateGetRowsRequest(pageNo, pageSize, orderByColumnName, orderBySort);
        var resp = await _client.GetRowsAsync(req, AppGrpcSession.Headers!);
        var rows = new List<PersonPersonphone>();
        foreach (var row in resp.Rows)
        {
            rows.Add(MapRowToTableEntity(row));
        }
        return new PersonPersonphoneRowsData
        {
            Success = resp.Success,
            Rows = rows,
            FromCache = resp.FromCache,
            Message = resp.Message
        };
    }

    public async Task<PersonPersonphoneInsertedData> InsertAsync(PersonPersonphone entity, bool modifyCache = true)
    {
        var req = base.CreateInsertRequest(entity, modifyCache);
        var resp = await _client.InsertAsync(req, AppGrpcSession.Headers!);
        return new PersonPersonphoneInsertedData
        {
            Success = resp.Success,
            InsertedRow = resp.InsertedRow != null ? MapRowToTableEntity(resp.InsertedRow) : null,
            CacheUpdated = resp.CacheUpdated,
            Message = resp.Message
        };
    }

    public async Task<PersonPersonphoneRowData> GetAsync(int businessentityid, string phonenumber, int phonenumbertypeid, bool useCache = true)
    {
        var req = base.CreateGetRequest();
        req.PrimaryKeyValues.Add("businessentityid", businessentityid.ToString());
        req.PrimaryKeyValues.Add("phonenumber", phonenumber.ToString());
        req.PrimaryKeyValues.Add("phonenumbertypeid", phonenumbertypeid.ToString());
        var resp = await _client.GetAsync(req, AppGrpcSession.Headers!);
        return new PersonPersonphoneRowData
        {
            Success = resp.Success,
            Row = resp.Row != null ? MapRowToTableEntity(resp.Row) : null,
            FromCache = resp.FromCache,
            Message = resp.Message
        };
    }

    public async Task<UpdatedData> UpdateAsync(PersonPersonphone entity, bool modifyCache = true)
    {
        var req = CreateUpdateRequest(entity, modifyCache);
        return await UpdateCoreAsync(req);
    }

    protected override PersonPersonphone MapRowToTableEntity(Row row)
    {
        var dict = new Dictionary<string, string?>();
        foreach (var entry in row.Entries)
        {
            dict[entry.Column] = entry.Value?.StringValue;
        }
        return new PersonPersonphone
        {
            Businessentityid = dict.TryGetValue("businessentityid", out var v1) && !string.IsNullOrEmpty(v1) ? (int)Convert.ChangeType(v1, typeof(int)) : default(int),
            Phonenumber = dict.TryGetValue("phonenumber", out var v2) && !string.IsNullOrEmpty(v2) ? (string)Convert.ChangeType(v2, typeof(string)) : default(string),
            Phonenumbertypeid = dict.TryGetValue("phonenumbertypeid", out var v3) && !string.IsNullOrEmpty(v3) ? (int)Convert.ChangeType(v3, typeof(int)) : default(int),
            Modifieddate = dict.TryGetValue("modifieddate", out var v4) && !string.IsNullOrEmpty(v4) ? (DateTime)Convert.ChangeType(v4, typeof(DateTime)) : default(DateTime),
        };
    }

    protected override Row MapTableEntityToRow(PersonPersonphone entity, DbOperationType dbOperationType)
    {
        var row = new Row();

        row.Entries.Add(new RowEntry { Column = "businessentityid", Value = new Value { StringValue = entity.Businessentityid.ToString() } });
        row.Entries.Add(new RowEntry { Column = "phonenumber", Value = new Value { StringValue = entity.Phonenumber.ToString() } });
        row.Entries.Add(new RowEntry { Column = "phonenumbertypeid", Value = new Value { StringValue = entity.Phonenumbertypeid.ToString() } });

        // For Postgres, add all non-primary key columns
        if (entity.Modifieddate != DateTime.MinValue)
        {
            row.Entries.Add(new RowEntry { Column = "modifieddate", Value = new Value { StringValue = entity.Modifieddate.ToString("yyyy-MM-dd HH:mm:ss.fff") } });
        }
        return row;
    }
}
