using Newtonsoft.Json;
using Newtonsoft.Json.Linq;
using RedflyCoreFramework;
using redflyDatabaseAdapters;
using redflyGeneratedDataAccessApi.Base;
using redflyGeneratedDataAccessApi.Common;
using redflyGeneratedDataAccessApi.Protos.DatabaseApi;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace redflyGeneratedDataAccessApi.Postgres.AdventureWorks;

// Strongly-typed classes for [humanresources].[employeepayhistory] generated by the
// redfly PostgresGrpcPolyLangCompiler on 06/06/25 04:21:27 PM
// This is only meant to be indicative of the features available in the core product.

public class HumanresourcesEmployeepayhistory : BasePostgresTableSchema
{
    public int Businessentityid { get; set; }
    public DateTime Ratechangedate { get; set; }
    public decimal Rate { get; set; }
    public short Payfrequency { get; set; }
    public DateTime Modifieddate { get; set; }
}

public class HumanresourcesEmployeepayhistoryRowsData : BaseTableRowsData
{
    public List<HumanresourcesEmployeepayhistory> Rows { get; set; } = new();
}

public class HumanresourcesEmployeepayhistoryInsertedData : BaseTableInsertedData
{
    public HumanresourcesEmployeepayhistory? InsertedRow { get; set; }
}

public class HumanresourcesEmployeepayhistoryRowData : BaseTableRowData
{
    public HumanresourcesEmployeepayhistory? Row { get; set; }
}

public class HumanresourcesEmployeepayhistoryDataSource : BasePostgresTableDataSource<HumanresourcesEmployeepayhistory>
{
    public HumanresourcesEmployeepayhistoryDataSource() : base()
    {
        _encSchema = RedflyEncryption.EncryptToString("humanresources");
        _encTable = RedflyEncryption.EncryptToString("employeepayhistory");
    }

    public async Task<DeletedData> DeleteAsync(int businessentityid, DateTime ratechangedate, bool modifyCache = true)
    {
        var req = base.CreateDeleteRequest(modifyCache);
        req.PrimaryKeyValues.Add("businessentityid", businessentityid.ToString());
        req.PrimaryKeyValues.Add("ratechangedate", ratechangedate.ToString());
        return await base.DeleteCoreAsync(req);
    }

    public async Task<HumanresourcesEmployeepayhistoryRowsData> GetRowsAsync(int pageNo = 1, int pageSize = 50, string orderByColumnName = "", string orderBySort = "", bool useCache = true)
    {
        var req = base.CreateGetRowsRequest(pageNo, pageSize, orderByColumnName, orderBySort);
        var resp = await _client.GetRowsAsync(req, AppGrpcSession.Headers!);
        var rows = new List<HumanresourcesEmployeepayhistory>();
        foreach (var row in resp.Rows)
        {
            rows.Add(MapRowToTableEntity(row));
        }
        return new HumanresourcesEmployeepayhistoryRowsData
        {
            Success = resp.Success,
            Rows = rows,
            FromCache = resp.FromCache,
            Message = resp.Message
        };
    }

    public async Task<HumanresourcesEmployeepayhistoryInsertedData> InsertAsync(HumanresourcesEmployeepayhistory entity, bool modifyCache = true)
    {
        var req = base.CreateInsertRequest(entity, modifyCache);
        var resp = await _client.InsertAsync(req, AppGrpcSession.Headers!);
        return new HumanresourcesEmployeepayhistoryInsertedData
        {
            Success = resp.Success,
            InsertedRow = resp.InsertedRow != null ? MapRowToTableEntity(resp.InsertedRow) : null,
            CacheUpdated = resp.CacheUpdated,
            Message = resp.Message
        };
    }

    public async Task<HumanresourcesEmployeepayhistoryRowData> GetAsync(int businessentityid, DateTime ratechangedate, bool useCache = true)
    {
        var req = base.CreateGetRequest();
        req.PrimaryKeyValues.Add("businessentityid", businessentityid.ToString());
        req.PrimaryKeyValues.Add("ratechangedate", ratechangedate.ToString());
        var resp = await _client.GetAsync(req, AppGrpcSession.Headers!);
        return new HumanresourcesEmployeepayhistoryRowData
        {
            Success = resp.Success,
            Row = resp.Row != null ? MapRowToTableEntity(resp.Row) : null,
            FromCache = resp.FromCache,
            Message = resp.Message
        };
    }

    public async Task<UpdatedData> UpdateAsync(HumanresourcesEmployeepayhistory entity, bool modifyCache = true)
    {
        var req = CreateUpdateRequest(entity, modifyCache);
        return await UpdateCoreAsync(req);
    }

    protected override HumanresourcesEmployeepayhistory MapRowToTableEntity(Row row)
    {
        var dict = new Dictionary<string, string?>();
        foreach (var entry in row.Entries)
        {
            dict[entry.Column] = entry.Value?.StringValue;
        }
        return new HumanresourcesEmployeepayhistory
        {
            Businessentityid = dict.TryGetValue("businessentityid", out var v1) && !string.IsNullOrEmpty(v1) ? (int)Convert.ChangeType(v1, typeof(int)) : default(int),
            Ratechangedate = dict.TryGetValue("ratechangedate", out var v2) && !string.IsNullOrEmpty(v2) ? (DateTime)Convert.ChangeType(v2, typeof(DateTime)) : default(DateTime),
            Rate = dict.TryGetValue("rate", out var v3) && !string.IsNullOrEmpty(v3) ? (decimal)Convert.ChangeType(v3, typeof(decimal)) : default(decimal),
            Payfrequency = dict.TryGetValue("payfrequency", out var v4) && !string.IsNullOrEmpty(v4) ? (short)Convert.ChangeType(v4, typeof(short)) : default(short),
            Modifieddate = dict.TryGetValue("modifieddate", out var v5) && !string.IsNullOrEmpty(v5) ? (DateTime)Convert.ChangeType(v5, typeof(DateTime)) : default(DateTime),
        };
    }

    protected override Row MapTableEntityToRow(HumanresourcesEmployeepayhistory entity, DbOperationType dbOperationType)
    {
        var row = new Row();

        row.Entries.Add(new RowEntry { Column = "businessentityid", Value = new Value { StringValue = entity.Businessentityid.ToString() } });
        row.Entries.Add(new RowEntry { Column = "ratechangedate", Value = new Value { StringValue = entity.Ratechangedate.ToString() } });

        // For Postgres, add all non-primary key columns
        row.Entries.Add(new RowEntry { Column = "rate", Value = new Value { StringValue = entity.Rate.ToString() } });
        row.Entries.Add(new RowEntry { Column = "payfrequency", Value = new Value { StringValue = entity.Payfrequency.ToString() } });
        if (entity.Modifieddate != DateTime.MinValue)
        {
            row.Entries.Add(new RowEntry { Column = "modifieddate", Value = new Value { StringValue = entity.Modifieddate.ToString("yyyy-MM-dd HH:mm:ss.fff") } });
        }
        return row;
    }
}
