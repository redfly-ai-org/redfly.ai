using Newtonsoft.Json;
using Newtonsoft.Json.Linq;
using RedflyCoreFramework;
using redflyDatabaseAdapters;
using redflyGeneratedDataAccessApi.Base;
using redflyGeneratedDataAccessApi.Common;
using redflyGeneratedDataAccessApi.Protos.DatabaseApi;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace redflyGeneratedDataAccessApi.Postgres.AdventureWorks;

// Strongly-typed classes for [sales].[countryregioncurrency] generated by the
// redfly PostgresGrpcPolyLangCompiler on 06/06/25 04:21:28 PM
// This is only meant to be indicative of the features available in the core product.

public class SalesCountryregioncurrency : BasePostgresTableSchema
{
    public string Countryregioncode { get; set; } = string.Empty;
    public string Currencycode { get; set; } = string.Empty;
    public DateTime Modifieddate { get; set; }
}

public class SalesCountryregioncurrencyRowsData : BaseTableRowsData
{
    public List<SalesCountryregioncurrency> Rows { get; set; } = new();
}

public class SalesCountryregioncurrencyInsertedData : BaseTableInsertedData
{
    public SalesCountryregioncurrency? InsertedRow { get; set; }
}

public class SalesCountryregioncurrencyRowData : BaseTableRowData
{
    public SalesCountryregioncurrency? Row { get; set; }
}

public class SalesCountryregioncurrencyDataSource : BasePostgresTableDataSource<SalesCountryregioncurrency>
{
    public SalesCountryregioncurrencyDataSource() : base()
    {
        _encSchema = RedflyEncryption.EncryptToString("sales");
        _encTable = RedflyEncryption.EncryptToString("countryregioncurrency");
    }

    public async Task<DeletedData> DeleteAsync(string countryregioncode, string currencycode, bool modifyCache = true)
    {
        var req = base.CreateDeleteRequest(modifyCache);
        req.PrimaryKeyValues.Add("countryregioncode", countryregioncode.ToString());
        req.PrimaryKeyValues.Add("currencycode", currencycode.ToString());
        return await base.DeleteCoreAsync(req);
    }

    public async Task<SalesCountryregioncurrencyRowsData> GetRowsAsync(int pageNo = 1, int pageSize = 50, string orderByColumnName = "", string orderBySort = "", bool useCache = true)
    {
        var req = base.CreateGetRowsRequest(pageNo, pageSize, orderByColumnName, orderBySort);
        var resp = await _client.GetRowsAsync(req, AppGrpcSession.Headers!);
        var rows = new List<SalesCountryregioncurrency>();
        foreach (var row in resp.Rows)
        {
            rows.Add(MapRowToTableEntity(row));
        }
        return new SalesCountryregioncurrencyRowsData
        {
            Success = resp.Success,
            Rows = rows,
            FromCache = resp.FromCache,
            Message = resp.Message
        };
    }

    public async Task<SalesCountryregioncurrencyInsertedData> InsertAsync(SalesCountryregioncurrency entity, bool modifyCache = true)
    {
        var req = base.CreateInsertRequest(entity, modifyCache);
        var resp = await _client.InsertAsync(req, AppGrpcSession.Headers!);
        return new SalesCountryregioncurrencyInsertedData
        {
            Success = resp.Success,
            InsertedRow = resp.InsertedRow != null ? MapRowToTableEntity(resp.InsertedRow) : null,
            CacheUpdated = resp.CacheUpdated,
            Message = resp.Message
        };
    }

    public async Task<SalesCountryregioncurrencyRowData> GetAsync(string countryregioncode, string currencycode, bool useCache = true)
    {
        var req = base.CreateGetRequest();
        req.PrimaryKeyValues.Add("countryregioncode", countryregioncode.ToString());
        req.PrimaryKeyValues.Add("currencycode", currencycode.ToString());
        var resp = await _client.GetAsync(req, AppGrpcSession.Headers!);
        return new SalesCountryregioncurrencyRowData
        {
            Success = resp.Success,
            Row = resp.Row != null ? MapRowToTableEntity(resp.Row) : null,
            FromCache = resp.FromCache,
            Message = resp.Message
        };
    }

    public async Task<UpdatedData> UpdateAsync(SalesCountryregioncurrency entity, bool modifyCache = true)
    {
        var req = CreateUpdateRequest(entity, modifyCache);
        return await UpdateCoreAsync(req);
    }

    protected override SalesCountryregioncurrency MapRowToTableEntity(Row row)
    {
        var dict = new Dictionary<string, string?>();
        foreach (var entry in row.Entries)
        {
            dict[entry.Column] = entry.Value?.StringValue;
        }
        return new SalesCountryregioncurrency
        {
            Countryregioncode = dict.TryGetValue("countryregioncode", out var v1) && !string.IsNullOrEmpty(v1) ? (string)Convert.ChangeType(v1, typeof(string)) : default(string),
            Currencycode = dict.TryGetValue("currencycode", out var v2) && !string.IsNullOrEmpty(v2) ? (string)Convert.ChangeType(v2, typeof(string)) : default(string),
            Modifieddate = dict.TryGetValue("modifieddate", out var v3) && !string.IsNullOrEmpty(v3) ? (DateTime)Convert.ChangeType(v3, typeof(DateTime)) : default(DateTime),
        };
    }

    protected override Row MapTableEntityToRow(SalesCountryregioncurrency entity, DbOperationType dbOperationType)
    {
        var row = new Row();

        row.Entries.Add(new RowEntry { Column = "countryregioncode", Value = new Value { StringValue = entity.Countryregioncode.ToString() } });
        row.Entries.Add(new RowEntry { Column = "currencycode", Value = new Value { StringValue = entity.Currencycode.ToString() } });

        // For Postgres, add all non-primary key columns
        if (entity.Modifieddate != DateTime.MinValue)
        {
            row.Entries.Add(new RowEntry { Column = "modifieddate", Value = new Value { StringValue = entity.Modifieddate.ToString("yyyy-MM-dd HH:mm:ss.fff") } });
        }
        return row;
    }
}
